name: Build Tank Arena APK

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'tank-arena-build/**'
      - '.github/workflows/tank-arena-apk.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Restore source archive
        run: |
          mkdir -p build-src
          base64 -d tank-arena-build/tank-arena-android.tar.gz.b64 | tar -xz -C build-src

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android platform
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build installable debug APK
        working-directory: build-src/tank-arena-android
        run: gradle :app:assembleDebug --stacktrace

      - name: Rename APK
        run: cp build-src/tank-arena-android/app/build/outputs/apk/debug/app-debug.apk Tank-Arena-1.0.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Tank-Arena-APK
          path: Tank-Arena-1.0.apk
          if-no-files-found: error
          retention-days: 7
